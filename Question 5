#include <iostream>
#include <stack>
#include <string>
using namespace std;
int evaluate(string a){
 stack <int> b;

 
int n=a.length();

 for (int i=0;i<n;i++){
    if (isdigit(a[i])){
        
        string num=string(1,a[i]);
        int num2=stoi(num);
        b.push(num2);}

    
    else if(!isdigit(a[i])){
        if(a[i]=='+'){
            int num3=b.top();
            b.pop();
            int num4=b.top();
            b.pop();
            b.push(num4+num3);
            
        }
       else  if(a[i]=='-'){
            int num3=b.top();
            b.pop();
            int num4=b.top();
            b.pop();
            b.push(num4-num3);
            
        }

       else if(a[i]=='/'){
            int num3=b.top();
            b.pop();
            int num4=b.top();
            b.pop();
            b.push(num4/num3);
            
        }

       else if(a[i]=='*'){
            int num3=b.top();
            b.pop();
            int num4=b.top();
            b.pop();
            b.push(num4*num3);
            
        }
        else if(a[i]=='^'){
            int num3=b.top();
            b.pop();
            int num4=b.top();
            b.pop();
            b.push(num4^num3);
            
        }

    }
}
return b.top();
    
    
}


int main(){
string a="52+83-*";

    int b=evaluate(a);
    cout<<b;

}
